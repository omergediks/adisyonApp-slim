import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.omergedik.adisyon',
  appName: 'AdisyonApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
